// Server configuration
// Change this value when hosting the server elsewhere
// For production, you can set it via environment variable: import.meta.env.VITE_SERVER_HOST
const SERVER_HOST = import.meta.env.VITE_SERVER_HOST || 'http://localhost:5000';

export default SERVER_HOST;
